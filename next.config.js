/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      appDir: true,
    },
  }
  
  module.exports = nextConfig

  //configuración para poder utilizar directorios nombrados como "app"